[pytest]
testpaths = tests
addopts = -v --tb=short --alluredir=reports/allure-results --clean-alluredir
markers =
    ui: functional browser UI tests
    api_ui: tests that combine API preparation/verification with UI actions
bdd_features_base_dir = features
